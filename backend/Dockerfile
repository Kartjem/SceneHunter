# 1. Берем за основу официальный образ Node.js 20
FROM node:20-slim

# 2. Устанавливаем рабочую папку внутри контейнера
WORKDIR /usr/src/app

# 3. Копируем package.json, чтобы установить зависимости
COPY package*.json ./

# 4. Устанавливаем все зависимости, нужные для работы сервера
RUN npm install

# 5. Копируем весь остальной код нашего бэкенда
COPY . .

# 6. Собираем TypeScript в JavaScript
RUN npm run build

# 7. Открываем порт, на котором будет работать сервер
EXPOSE 3001

# 8. Финальная команда для запуска сервера
CMD [ "node", "dist/index.js" ]